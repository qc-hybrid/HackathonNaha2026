remote_theme: jekyll/minima@d4d779a
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

# Site Settings
locale                   : "en"
baseurl: "/CompPhysHack2026"

# Disable GitHub repository link
github:
  is_project_page: false